# Project Rules & Instructions

## GitHub Workflow & Deployment
- **DO NOT** commit or push changes to GitHub automatically after edits.
- Only commit and push changes when the user explicitly uses the keyword **"guarda"** (or asks to save to GitHub).
- When the user says **"guarda"**, push the changes and create/update the Pull Request on GitHub.

## Git Configuration & Identity
- Always use **`KELEK HOME`** as `user.name` and **`kelek.home@gmail.com`** as `user.email` for all Git operations, commits, and pushes.